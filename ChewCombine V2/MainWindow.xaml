<Window x:Class="ChewCombine_V2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChewCombine_V2"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="ChewCombine v2.02-beta" Height="600" Width="1000"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="1.5*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" 
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="15">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center">
                    <ui:TextBlock Text="ChewCombine" FontTypography="TitleLarge" FontWeight="Bold" Margin="0,0,0,8" HorizontalAlignment="Center"/>

                    <ui:TextBlock Text="(Click me) GitHub: YakumoZn/ChewCombine-v2" 
                  FontSize="12" 
                  Foreground="Gray" 
                  Cursor="Hand" 
                  HorizontalAlignment="Center" 
                  Margin="0,0,0,4" 
                  MouseLeftButtonUp="GitHubLink_Click"/>

                    <ui:TextBlock x:Name="BeatmapCountText" Text="当前谱面数: 0" FontSize="16" Foreground="Gray" HorizontalAlignment="Center"/>
                </StackPanel>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <ui:Button Content="设置" Margin="0,0,10,0" Click="SettingsButton_Click">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Settings24"/>
                            </ui:Button.Icon>
                        </ui:Button>
                        <ui:Button Content="合成" Appearance="Primary" Click="CombineButton_Click">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Wrench24"/>
                            </ui:Button.Icon>
                        </ui:Button>
                    </StackPanel>

                    <ListBox x:Name="LeftListBox" Grid.Row="1" BorderThickness="0" Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <ui:Button Grid.Column="0" 
                               Padding="4"
                               Appearance="Transparent"
                               Click="ToggleButton_Click">
                                            <ui:SymbolIcon Symbol="ChevronDown24" />
                                        </ui:Button>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                            <ui:TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis" />
                                            <ui:TextBlock Text="{Binding DisplaySubtitle}" Foreground="Gray" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <ui:Button Grid.Column="2" Margin="10,0,10,0" Appearance="Danger" Click="RemoveButton_Click">
                                            <ui:SymbolIcon Symbol="Dismiss24"/>
                                        </ui:Button>
                                    </Grid>

                                    <Grid Margin="10,10,10,5" 
                                        Visibility="{Binding IsExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                            <ui:TextBlock Text="起始位置 (ms)" FontSize="12" Margin="0,0,0,4" Opacity="0.7"/>
                                            <ui:TextBox Text="{Binding StartTime, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                            <ui:TextBlock Text="结束位置 (ms)" FontSize="12" Margin="0,0,0,4" Opacity="0.7"/>
                                            <ui:TextBox Text="{Binding EndTime, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                    </Grid>

                                    <Separator Margin="0,5,0,0" Opacity="0.2"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Column="2" 
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="15">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>

                    <ui:TextBox x:Name="SearchBox" Grid.Column="0" PlaceholderText="搜索谱面..." TextChanged="SearchBox_TextChanged">
                        <ui:TextBox.Icon>
                            <ui:SymbolIcon Symbol="Search24"/>
                        </ui:TextBox.Icon>
                    </ui:TextBox>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:Button ToolTip="刷新" Margin="0,0,8,0" Click="RefreshButton_Click">
                            <ui:SymbolIcon Symbol="ArrowClockwise24"/>
                        </ui:Button>
                        <ui:Button x:Name="OpenFolderButton" ToolTip="open osu Songs" Click="SelectSongsFolder_Click">
                            <ui:SymbolIcon Symbol="Folder24"/>
                        </ui:Button>
                    </StackPanel>
                </Grid>

                <ListBox x:Name="RightListBox" Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="0,10,0,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="10,0,0,0">
                                    <ui:TextBlock Text="{Binding Title}" 
                                              FontWeight="SemiBold" 
                                              FontSize="16" 
                                              TextTrimming="CharacterEllipsis" 
                                              ToolTip="{Binding Title}" />
                                    <ui:TextBlock Text="{Binding DisplaySubtitle}" 
                                              Foreground="Gray" 
                                              FontSize="12" 
                                              Margin="0,4,0,0" 
                                              TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <ui:Button Grid.Column="1" 
                                       Margin="10,0,10,0" 
                                       Appearance="Secondary"
                                       VerticalAlignment="Center" 
                                       Click="AddButton_Click">
                                    <ui:SymbolIcon Symbol="Add24"/>
                                </ui:Button>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>